def area():
	return None

def poparea():
	print("meow")

	# SELECT blocks in CMAs AS source_blocks
	# CLIP by LAND
	# ADD back in any BLOCKS removed
	# ADD in pop, dwellings

	# compute area of block
	# compute total pop of source CT, join to block
	# union blocks to target CT
	# compute area of segments, Aint / Ak
	# CTi, CTj, Biarea, Bijarea, Pbi, Pcti
	# group by CTi, CTj
	# weight = sum (Bijarea / Biarea) (Pbi / Pcti)

	return None


